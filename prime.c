#include <stdio.h>

int main()
{
    int i,j,num;
    printf("enter a number to checkinng prime or not? : ");
    scanf("%d",&num);
    if(num%2!=0,num%3!=0)
    printf("%d is prime.",num);
    else
    printf("%d is not prime.",num);

    return 0;
}
